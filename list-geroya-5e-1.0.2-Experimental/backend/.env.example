DATABASE_URL=sqlite:///./data/list-geroya.db
APP_BASE_URL=https://example.invalid
SESSION_DAYS=30
PASSWORD_ITERATIONS=310000
VAULT_MAX_BYTES=2000000
EMAIL_VERIFICATION_ENABLED=false
REQUIRE_VERIFIED_EMAIL=false
EMAIL_DELIVERY_ENABLED=false
PASSWORD_RESET_ENABLED=true
REGISTRATION_ENABLED=true
LOGIN_ENABLED=true
# Mail.ru SMTP (or another SMTP service). Keep the real password only in the host's secret settings.
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USERNAME=
SMTP_PASSWORD=
RESEND_API_KEY=
EMAIL_FROM=
COOKIE_SECURE=true
